version: '3.8'
services:
  web:
    image: nginx:stable-alpine
    ports:
      - target: 80
        published: 8081
        protocol: tcp
        mode: host
    deploy:
      replicas: 1
      placement:
        constraints:
          - node.platform.os == linux
